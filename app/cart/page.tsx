'use client';
import { useEffect, useMemo, useState } from 'react';
import SaffaHeader,{useSaffaLanguage} from '../components/SaffaHeader';
import { products } from '../../lib-products';
import './cart.css';

type CartItem={slug:string;name:string;image:string;price:number;quantity:number;color?:string;arColor?:string;size?:string};
export default function CartPage(){const [language]=useSaffaLanguage();const [cart,setCart]=useState<CartItem[]>([]);const ar=language==='ar';const text=ar?{cart:'سلة التسوق',item:'منتج',items:'منتجات',remove:'حذف',total:'الإجمالي',clear:'إفراغ السلة',buy:'شراء الآن عبر واتساب',checkout:'إتمام الطلب',empty:'سلتك فارغة',continue:'متابعة التسوق',add:'اختاري منتجًا من المجموعة أولاً'}:{cart:'Your Cart',item:'item',items:'items',remove:'Remove',total:'Total',clear:'Clear Cart',buy:'Buy Now via WhatsApp',checkout:'Checkout',empty:'Your cart is empty',continue:'Continue Shopping',add:'Choose a product from the collection first'};useEffect(()=>{try{const saved=JSON.parse(localStorage.getItem('saffa-cart')||'[]') as CartItem[];setCart(saved.map(item=>{const p=products.find(x=>x.slug===item.slug);return {...item,price:p?.price??item.price,color:item.color||p?.color,arColor:item.arColor||p?.arColor};}));}catch{setCart([])}},[]);useEffect(()=>{try{localStorage.setItem('saffa-cart',JSON.stringify(cart));}catch{}} , [cart]);const total=useMemo(()=>cart.reduce((sum,item)=>sum+item.price*item.quantity,0),[cart]);function changeQuantity(slug:string,delta:number){setCart(current=>current.map(item=>item.slug===slug?{...item,quantity:item.quantity+delta}:item).filter(item=>item.quantity>0));}function removeItem(slug:string){setCart(current=>current.filter(item=>item.slug!==slug));}function clearCart(){setCart([]);}return <main className="saffa-cart-page"><SaffaHeader active="home" cartCount={cart.reduce((sum,item)=>sum+item.quantity,0)} cartAction={()=>{}}/><section className="cart-page-inner"><div className="cart-page-heading"><span>SAFFA FASHION</span><h1>{text.cart}</h1><p>{cart.length} {cart.length===1?text.item:text.items}</p></div>{!cart.length?<div className="cart-empty-state"><div>0</div><h2>{text.empty}</h2><p>{text.add}</p><a href="/grid">{text.continue} →</a></div>:<><div className="cart-list">{cart.map(item=><article className="cart-page-item" key={item.slug}><div className="cart-page-image"><img src={item.image} alt={item.name}/></div><div className="cart-page-info"><span className="cart-page-color">{ar?(item.arColor||item.color):item.color}</span><h2>{ar?(products.find(p=>p.slug===item.slug)?.arName||item.name):item.name}</h2><strong>EGP {item.price.toFixed(2)}</strong><small>{ar?'المقاس':'Size'}: {item.size||'—'}</small><div className="cart-page-quantity"><button type="button" onClick={()=>changeQuantity(item.slug,-1)}>−</button><b>{item.quantity}</b><button type="button" onClick={()=>changeQuantity(item.slug,1)}>+</button></div></div><button className="cart-page-remove" type="button" onClick={()=>removeItem(item.slug)}>{text.remove}</button></article>)}</div><div className="cart-page-total"><span>{text.total}</span><strong>EGP {total.toFixed(2)}</strong></div><div className="cart-page-actions"><a className="cart-whatsapp" href="/checkout">{text.checkout} →</a><button className="cart-clear" type="button" onClick={clearCart}>{text.clear}</button></div></>}</section><a className="cart-floating-whatsapp" href="https://wa.me/201069473693" target="_blank" rel="noreferrer">💬 <span>WhatsApp</span></a></main>;
}
